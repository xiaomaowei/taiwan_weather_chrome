# 🌦️ 台灣天氣可愛手繪風 Chrome 擴充功能 (Taiwan Weather Chrome Extension)

這是一款專為台灣設計的 Chrome 瀏覽器天氣擴充功能。採用**溫馨可愛的手繪插畫風格**，並支援細緻到**鄉鎮區級別**的即時天氣與一週預報，讓你在瀏覽網頁之餘，也能隨時掌握全台各地的天氣變化！

<img width="360" height="570" alt="image" src="https://github.com/user-attachments/assets/19584ea4-4679-43a5-b2d7-786f9a141027" />



---

## ✨ 核心特色

* 📍 **細緻定位**：支援全台灣 22 個縣市、所有鄉鎮市區（例如：台北市北投區、新北市新店區）。
* 🌦️ **即時天氣資訊**：提供當前溫度、體感溫度、相對濕度、降雨機率、風力級別與風向。
* 📅 **一週預報**：直觀的 7 天天氣預報，包含星期、天氣狀況插圖、高低溫差及降雨機率。
* 🎨 **療癒手繪設計**：精心調配的暖色系/深色系配色，搭配可愛的手繪風邊框與微動畫，畫面精美、使用體驗流暢。
* 🏷️ **常用地區快速切換**：整合模糊搜尋框，打字即時篩選，一鍵儲存常用鄉鎮，免去繁瑣的重複設定。
* 🌡️ **工具列圖示即時溫標**：免點開！瀏覽器工具列圖示直接大字顯示主地區的當前溫度，並根據氣溫自動變換不同背景顏色（寒冷藍、溫暖黃、炎熱橘等）。
* ⚡ **省流快取與效能優化**：內建 Service Worker 背景定時快取機制（TTL 可自訂），避免重複請求氣象署 API，節省每日流量。

---

## 🛠️ 技術架構

* **前端核心**：HTML5, JavaScript (ES6+), Vanilla CSS（採用手繪風格卡片與靈活佈局）
* **擴充功能標準**：Chrome Extension Manifest V3
* **背景服務**：Service Worker (`background.js`)、Chrome Alarms API、Chrome Storage API
* **氣象數據來源**：中華民國交通部中央氣象署 (CWA) 開放資料 API

---

## 📦 安裝教學

1. **下載本專案**：
   * 點擊本倉庫右上角的 `Code` -> `Download ZIP` 並解壓縮，或者使用 Git 複製倉庫：
     ```bash
     git clone https://github.com/xiaomaowei/taiwan_weather_chrome.git
     ```
2. **開啟 Chrome 擴充功能頁面**：
   * 在 Chrome 網址列輸入 `chrome://extensions/` 並回車。
3. **啟用開發者模式**：
   * 將擴充功能頁面右上角的**「開發者模式」**開關切換為啟用。
4. **載入擴充功能**：
   * 點擊左上角的**「載入未封裝擴充功能」**按鈕。
   * 選擇本專案的解壓縮資料夾（包含 `manifest.json` 的該層目錄）。
5. **固定至工具列**：
   * 點擊 Chrome 右上角的拼圖圖示 🧩，找到「台灣天氣」，點擊圖釘按鈕將其固定在工具列，以便隨時查看即時溫度！

---

## 🔑 申請與設定 CWA API Key

為了能順利獲取最即時的氣象資訊，建議設定您專屬的中央氣象署授權碼：

1. **申請授權碼**：
   * 前往 [交通部中央氣象署氣象會員會員登入平台](https://opendata.cwa.gov.tw/index) 註冊免費帳號。
   * 登入後在個人帳戶的「取得授權碼」頁面中複製您的 `API Key` (格式如：`CWA-XXXXXX`)。
2. **在擴充功能中設定**：
   * 點開瀏覽器上的台灣天氣擴充功能。
   * 點擊右上角的**「齒輪」設定按鈕**展開設定面板。
   * 在「API 授權碼」欄位貼上您申請的授權碼，點擊**「儲存設定」**。
   * *(註：本套件已預設內嵌一個共享授權碼以供快速體驗，但若每日 API 呼叫次數達上限，請自行更換為專屬授權碼。)*

---

## 📢 氣象資料限制與說明

* 💧 **一週預報降雨機率**：
  * **僅前三天有百分比數值**：依據中央氣象署（CWA）的逐 12 小時七天預報資料特性，降雨機率（ProbabilityOfPrecipitation）僅提供未來前 3 天的預測數值。
  * **後半週顯示為 `💧--`**：後半週（第 4 天至第 7 天）因時間較長、變數較多，氣象署 API 在此資料集中不提供降雨機率數值。本套件會自動將其顯示為 `💧--`，以符合真實資料狀態，避免顯示為 `0%` 造成誤導。

---

## 📂 專案檔案結構

```text
taiwan_weather_chrome/
├── manifest.json         # 擴充功能定義檔 (V3)
├── popup.html            # 擴充功能主視窗結構
├── popup.css             # 可愛手繪風格樣式表 (Light & Dark 雙主題)
├── popup.js              # 主視窗前端互動與 UI 渲染邏輯
├── background.js         # Service Worker，處理背景自動刷新與工具列 icon 繪製
├── city_county_data.js   # 台灣縣市與鄉鎮區資料對照表
├── icons/                # 擴充功能各尺寸圖示集
└── README.md             # 本說明文件
```

---

## 🎨 介面與主題切換

本套件支援**「手繪淺色主題」**與**「手繪深色主題」**。系統會根據您作業系統的深色模式設定自動套用，您也可以隨時在設定面板中手動切換：

* **淺色模式**：溫暖舒服的手繪羊皮紙米色底、炭灰色手繪線條與粉嫩的天氣插圖。
* **深色模式**：護眼的深夜黑板深藍底、明亮的手繪白線條。

---

## 🤝 貢獻與反饋

如果您在使用中遇到任何問題，或者有任何功能建議（例如想要更多可愛的天氣插圖、更多數據項目等），歡迎在 GitHub 提交 [Issue](https://github.com/xiaomaowei/taiwan_weather_chrome/issues) 或 Pull Request！

* **作者**：[xiaomaowei](https://github.com/xiaomaowei)
* **授權**：MIT License
