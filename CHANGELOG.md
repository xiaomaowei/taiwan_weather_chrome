# 更新日誌 (Change Log)

所有關於「台灣天氣可愛手繪風 Chrome 擴充功能」的重大更新與微調都記錄在此文件中。

---

## [v2.3.12] - 2026-06-29
### 新增
- **新增「12 小時雨量」趨勢條**：彈出視窗最下方新增一條雨量趨勢圖，顯示以目前時間為中心、往前 6 小時（實際）到往後 6 小時（預測）的逐時雨量長條，右上角以「💧 X.X mm」顯示該時段累積雨量。資料取自 Open-Meteo 逐時降雨（`hourly=precipitation`，搭配 `past_days=1`／`forecast_days=2`），過去與未來時段以不同顏色區分，並標出「現在」欄位。雨量陣列隨設定頁面的「資料更新頻率」一併取得並快取，但顯示視窗每次開啟都會重新對齊當下時間。背景腳本 (`background.js`) 的定時刷新也會一併抓取並快取此資料。
- **精簡彈出視窗版面**：為了讓新增的雨量趨勢條容納於單一畫面、不出現右側捲動軸，移除「一週天氣預報」與雨量區塊的標題文字（雨量區塊改以右上角的累積雨量數值表達），並微調卡片間距與內距，省下的垂直空間用來加高雨量長條，讓圖表更清楚。

## [v2.3.11] - 2026-06-29
### 變更
- **天氣詳情改顯示「風速/陣風」**：原本的「風力級別」欄位以蒲福風級（如「1 級」）呈現，現改為直接顯示中央氣象署即時觀測站的實際風速與陣風（如「2.3 / 5.4 m/s」），資訊更直覺且精確。陣風取自觀測 API (`O-A0003-001`) 的 `GustInfo.PeakGustSpeed`。連帶移除已不再使用的 `windSpeedToBeaufort` 蒲福風級轉換函式。升級時若快取為舊格式（缺少風速欄位）會自動失效並重新取得，避免顯示 `--`。
- **空氣品質併入今日氣象卡**：原本獨立的空氣品質卡片（含可展開的 PM10、O₃、NO₂、CO、SO₂ 與健康建議詳細面板）精簡為一條內嵌於氣象卡底部的資訊列，只顯示 AQI 與 PM2.5，並保留色彩狀態徽章（良好／普通…）與測站名稱。少一張卡片讓彈出視窗更精簡，同時移除已不再使用的詳細污染物面板相關程式與樣式。未設定環境部 API 金鑰時，該列會就地顯示設定提示。

## [v2.3.10] - 2026-06-18
### 變更
- **今日摘要雨勢分級優化**：今日摘要原本只在過去1小時雨量達 30mm 時才升級為「雨勢偏強」提示，門檻調降至 15mm，並新增 40mm 以上的「雨勢非常強」分級；同時新增降雨機率 ≥80% 時即顯示警示文案，不必等雨量數據追上實際下雨狀況，讓摘要更貼近劇烈天氣下的真實感受。

## [v2.3.9] - 2026-06-17
### 修正
- **修復 Release 發佈包缺少檔案**：CI 發布流程原本未複製 `i18n_data.js` 與 `_locales` 多語系資料夾，導致發布的 zip 套件無法正常顯示翻譯文字。現已修正打包腳本，確保所有必要檔案都包含在發布包內。
- **修正鬧鐘清除邏輯**：`background.js` 重新設定背景排程時，原本使用 `chrome.alarms.clearAll()` 會清除本擴充功能所有的鬧鐘設定；改為 `chrome.alarms.clear(ALARM_NAME)` 僅清除指定鬧鐘，避免未來新增其他排程時互相干擾。
### 重構
- **抽出共用天氣工具模組**：`background.js` 與 `popup.js` 原本各自維護一份天氣解析、雨量、即時觀測、Open-Meteo 補值、體感溫度、風級轉換等邏輯，容易修一邊漏一邊。現統一抽出至新增的 `weather_utils.js`，由背景腳本與彈出視窗共用，純粹為程式碼整理，不影響任何使用者可見的功能行為。

## [v2.3.8] - 2026-06-11
### 新增
- **當前觀測精確度提升**：整合中央氣象署觀測站即時觀測 API (`O-A0003-001`)。套件會自動媒合離您最近的氣象觀測站（以座標距離或鄉鎮比對），獲取過去 10 分鐘內最即時的溫度、體感溫度、濕度、風速、風向與天氣狀況，大幅提升即時天氣的精確度。
- **全螢幕設定面板與動畫優化**：重構設定覆蓋層，改為固定定位 (`fixed`) 的全螢幕滑出式面板。優化卡片尺寸與動畫，從原先的縮放彈出改為由底部平滑滑入的動畫效果，並在內容過長時啟用自訂手繪風滾動條，防範版面溢出。
- **背景代理 Fetch 優化**：將 Open-Meteo 後半週降雨機率的請求優先代理給 Service Worker 背景腳本處理，徹底解決 Manifest V3 下 Popup 的跨網域與主機權限 (Host Permissions) 限制問題，並保留本地 Fetch 作為相容性備用方案。

## [v2.3.7] - 2026-06-05
### 變更
- **視窗底邊空白縮減**：為解決前幾次佈局緊湊化後，在底部露出的多餘空白底板問題，將彈出視窗高度 (`body height`) 由 595px 進一步縮小至剛剛好的 **560px**，完美貼合預報底部並保留舒適呼吸感。

## [v2.3.6] - 2026-06-03
### 變更
- **一週預報 100% 降雨率排版微調**：為防止當降雨機率達三位數（如 100%）時在一週預報的極窄卡片中發生折行跑版，將降雨率的字型大小微調至 9px，並將圖示與數字的間距縮減為 0px，確保在限寬 35px 內依然能完美在同一行內顯示。

## [v2.3.5] - 2026-05-30
### 變更
- **今日摘要視覺微調**：為使介面更加融入，將右上角今日摘要的背景框與框線全面移除，直接以精簡字體顯示；同時移除文案尾端重複的天氣 emoji，僅保留前置的精緻小圖示，整體畫面更為乾淨素雅。

## [v2.3.4] - 2026-05-28
### 變更
- **摘要並排與 UI 箭頭放大**：為徹底根除直向滾動條，重構當前天氣佈局，將當前溫度與圖示靠左，並將「今日摘要」以小巧精緻的虛線框並排於右上角（字體調小至 11px），省去整行垂直空間；同時將空氣品質卡底部展開的倒三角圖示 (Expand Arrow) 放大至 16px 並提高對比度，大幅提升操作便利性。

## [v2.3.3] - 2026-05-25
### 變更
- **彈出視窗排版與尺寸優化**：顯式將擴充功能彈出視窗高度加長至 595px，並微調收緊了外容器、標題、卡片間距、摘要條的 padding 與 margin (共縮減 38px 垂直無效空間)，完美解決了直向滾動條露出的問題，介面更乾淨清爽。

## [v2.3.2] - 2026-05-20
### 變更
- **字型線上載入與優化**：改回使用 Google Fonts 線上連結加載 Fredoka 與 Outfit 英數字體，並移除高達 38MB 的本地 TTF/WOFF2 字型檔案，大幅縮減擴充功能的打包與安裝體積。
- **中文字型易讀性與排版優化**：繁體中文模式下中文字型改用系統內建高清晰度黑體（如微軟正黑體、PingFang SC），並全面調大字級 (font-size) 1~2px。徹底解決先前 Thin 手寫字太小太細及重影模糊的問題。同時將「今日摘要」重新設計排版，移入當前天氣主卡片（主框）內，版面更協調、不顯擁擠。

## [v2.3.0]
### 新增
- **氣象警特報整合**：即時抓取中央氣象署官方警特報 API (`W-C0033-001`)，支援豪雨、強風、低溫、高溫、颱風等官方警示。
- **今日智慧摘要**：根據當前溫度、降雨機率、時雨量、AQI 及警特報，自動產生一句話摘要。
- **詳細空氣品質數據**：展開空氣品質卡片可查看 PM10、O₃、NO₂、CO、SO₂ 以及健康建議。
- **API 金鑰快速測試**：設定面板新增「測試連線」按鈕。
